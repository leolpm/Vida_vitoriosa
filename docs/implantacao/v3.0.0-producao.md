# Implantacao v3.0.0 em producao

## 1. Identificacao

- Data: 2026-08-30
- Commit de aplicacao: `0856e0a8758bd085ad49fe7651569812cee802f7`
- Commit anterior: `b39cd3339fb07c30c25268b7a08cb68818d4b4e4`
- Branch de desenvolvimento: `codex/v3-fluxo-impressao`
- Branch de producao: `main`
- Estado: implantado e validado; limpeza final e tag pendentes

## 2. Validacao anterior ao deploy

- worktree local sem alteracoes
- promocao de `main` feita somente por fast-forward
- nenhuma mudanca em Composer, npm ou assets compilados
- rotas carregadas corretamente
- `php artisan test`: 49 testes e 444 assercoes aprovados

## 3. Backup temporario

Com a aplicacao em manutencao foi criado o conjunto privado:

```text
/home1/lionin25/igreja_vida_backups/igreja_vida_v3_20260830_231735
```

O conjunto possui 548.154.753 bytes e inclui:

- dump transacional e compactado do banco
- arquivo completo de `storage/app/public`
- manifesto SHA-256 dos arquivos publicos
- `.env` e `.htaccess`
- bundle e arquivo do codigo anterior
- estado Git e migrations anteriores
- checksums dos artefatos

O dump compactado possui 77.563 bytes, 17 tabelas, as tabelas criticas esperadas e marcador de conclusao. O servidor MySQL informou a restricao de privilegio `PROCESS` para metadados de tablespaces, sem impedir a geracao e a validacao do conteudo necessario.

## 4. Dados preservados

| Entidade | Antes | Depois |
|---|---:|---:|
| Usuarios | 3 | 3 |
| Participantes | 159 | 159 |
| Depoimentos | 225 | 225 |
| Lotes PDF | 165 | 165 |
| Arquivos em `storage/app/public` | 266 | 266 |

O armazenamento publico possuia 537.649.698 bytes. Os hashes SHA-256 dos 266 arquivos foram comparados depois da migration e permaneceram identicos. O symlink `public/storage` continuou apontando para `/home1/lionin25/Igreja_vida/storage/app/public`.

## 5. Migration e infraestrutura

A migration `2026_08_18_010000_create_print_flow_structure` foi aplicada com `php artisan migrate --force` e criou sete tabelas:

- `team_members`
- `event_team_member`
- `print_flows`
- `print_flow_testimonial`
- `print_flow_tokens`
- `print_flow_reviews`
- `print_flow_audits`

As sete tabelas iniciaram vazias. Nenhum seeder, `migrate:fresh`, `db:wipe`, reset ou limpeza de storage foi executado. O `.htaccess` do cPanel, os logs existentes e os arquivos nao versionados do servidor foram preservados.

## 6. Validacoes de producao

- Vida Vitoriosa redireciona HTTP para HTTPS com 301
- formulario e login do Vida Vitoriosa respondem 200 em HTTPS
- EDD redireciona HTTP para HTTPS com 301
- formulario e login do EDD respondem 200 em HTTPS
- aplicacao fora do modo de manutencao
- caches de configuracao, eventos, rotas e views recriados
- migration marcada como executada
- contagens do banco preservadas
- hashes do storage preservados
- nenhuma nova entrada `ERROR`, `CRITICAL`, `ALERT` ou `EMERGENCY` no log Laravel
- pagina publica do Vida Vitoriosa aberta no Chrome com o titulo `Enviar depoimento`

A homologacao funcional e visual completa havia sido concluida localmente pelo responsavel antes da autorizacao do deploy. A leitura automatizada detalhada do DOM em producao nao foi concluida por instabilidade da conexao com o navegador; nao foi executada alteracao de dados reais para contornar essa limitacao.

## 7. Reversao e limpeza

Foi criada a referencia local `server/pre-v3-20260830_231735` para o codigo anterior. A limpeza dos backups v2 e v3 foi autorizada pelo responsavel e sera executada somente depois deste registro estar publicado no GitHub e sincronizado no servidor.

Depois da limpeza, a reversao de codigo permanecera disponivel pelo Git. Nao existira copia privada do banco ou das imagens anterior a este deploy.

## 8. Historico de alteracoes

| Data | Alteracao | Motivo |
|---|---|---|
| 2026-08-30 | Registro da implantacao e validacao da v3.0.0 | Preservar evidencias de codigo, migration, dados, arquivos e testes antes da limpeza autorizada dos backups |
